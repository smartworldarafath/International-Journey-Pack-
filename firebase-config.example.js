// Rename this file to firebase-config.js and fill values before deploying
// __firebase_config must be a JSON string (because index.html uses JSON.parse(__firebase_config))
const __firebase_config = JSON.stringify({
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  // add other fields your Firebase console shows
});
const __app_id = "your-app-id";
const __initial_auth_token = ""; // optional: custom token for init auth
